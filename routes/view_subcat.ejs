<%- include('header') %>


            <!-- MAIN CONTENT-->
            <div class="main-content">
                <div class="section__content section__content--p30">
                    <div class="container-fluid" style="text-align: center;">
                        <div class="row">
                            <div class="col-lg-12">
                                <div class="table-responsive table--no-card m-b-30">
                                    <form action="/admin/comment/deletemulticomment" method="post">
                                        <table class="table table-borderless table-striped table-earning">
                                            <thead>
                                                <tr>
                                                    <th>No.</th>
                                                    <th>Category</th>
                                                    <th>Status</th>
                                                    <th>Action</th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                <% var i=1 %>
                                                <% for(cdata of categorydata){ %> 
                                                    <tr>
                                                        <td><%= i++ %></td>
                                                        <td><%= cdata.category %></td>
                                                        <td>
                                                            <% if(cdata.status){ %> 
                                                                <a href="/admin/category/deactive/<%= cdata.id %>" class="btn btn-success">Active</a>
                                                            <% }else{ %> 
                                                                <a href="/admin/category/active/<%= cdata.id %>" class="btn btn-danger">Deactive</a>
                                                            <% } %>
                                                        </td>
                                                        <td>
                                                            <a href="/admin/category/deletecomment/<%= cdata.id %>">
                                                                <i class="fa fa-trash"></i>
                                                            </a> ||
                                                            <a href="/admin/category/updatecomment/?id=<%= cdata.id %>">
                                                                <i class="fa fa-edit"></i>
                                                            </a>
                                                        </td>
                                                    </tr>
                                                <% } %>
                                            </tbody>
                                        </table>
                                    </form>
                                </div>
                            </div>
                        </div>

                        

<%- include('footer') %>    